import { createIcon } from '../createIcon';

export const Target = createIcon('Target', [
  ['circle', { cx: 12, cy: 12, r: 10, key: "svg-0" }],
  ['circle', { cx: 12, cy: 12, r: 6, key: "svg-1" }],
  ['circle', { cx: 12, cy: 12, r: 2, key: "svg-2" }],
]);

export default Target;
