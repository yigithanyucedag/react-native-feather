import { createIcon } from '../createIcon';

export const CreditCard = createIcon('CreditCard', [
  ['rect', { x: 1, y: 4, width: 22, height: 16, rx: 2, ry: 2, key: "svg-0" }],
  ['line', { x1: 1, y1: 10, x2: 23, y2: 10, key: "svg-1" }],
]);

export default CreditCard;
